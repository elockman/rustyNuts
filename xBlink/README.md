### Create Project Structure: Ensure your project structure looks like this:
src/
  main.rs
  gpio_blinker.rs
Cargo.toml

### Compile the Program:
cargo build --release

### Run the Program:
sudo ./target/release/<your_program_name>
